<!doctype html>
<html>
<head>
  <title>backflexjaxchatlot</title>
  <link rel="stylesheet" href="/assets/style.styl"></link>
  <script src="/assets/chat.coffee"></script>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.7.2/jquery.min.js"></script>
  <script charset="utf-8">
      $(document).ready(function(){
        chat = new Chat("ws://" + document.location.host + "/eventhub")
      });
  </script>
</head>
<body>
  <div class="container-fluid">
    <div class="left-sidebar">
    </div>
    <div class="main-content">
      <div class="chat-log">
        {% for message in messages | reverse %}
        <div class="message">
          <div class="message-author">{{ message["author"] }}</div>
          <div class="message-contents">{{ message["message"] }}</div>
        </div>
        {% endfor %}
      </div>
      <div class="input-container">
        <textarea class="chat-text"></textarea>
        <div class="chat-controls">
          <input class="chat-submit" type="button">Send</input>
          <div class="chat-control-spacer">
            <input class="chat-preview" type="button">Preview</input>
          </div>
        </div>
      </div>
    </div>
    <div class="right-sidebar">
    </div>
  </div>
</body>
</html>
