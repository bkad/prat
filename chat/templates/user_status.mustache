<div class="user-status {{ status }}" data-current-user="{{ isCurrentUser }}" style="background-image: url({{ gravatar }})">
  <span class="north" rel="tipsy" title="{{ username }}">{{ name }}</span>
  <div class="overlay"></div>
</div>
